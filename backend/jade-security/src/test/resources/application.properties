# Test-only AES key. Production must provide JADE_CRYPTO_MASTER_KEY.
jade.crypto.master-key=0123456789abcdef0123456789abcdef

# 单测只测加解密，不需要 DB
# 关掉 Hibernate ORM 避免 SysUser 等实体需要 datasource
quarkus.hibernate-orm.enabled=false
quarkus.datasource.enabled=false
